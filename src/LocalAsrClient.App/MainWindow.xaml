<Window x:Class="LocalAsrClient.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:infra="clr-namespace:LocalAsrClient.App.Infrastructure"
        Title="LessASR"
        Width="760"
        Height="520"
        MinWidth="640"
        MinHeight="420"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <TabControl SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="状态">
                <StackPanel Margin="16" Width="420" HorizontalAlignment="Left">
                    <TextBlock Text="当前状态" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <TextBlock Text="{Binding Status.CurrentState}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Status.CurrentModel}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Status.ServiceState}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Status.Hotkey}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Status.LastResult}" TextWrapping="Wrap"/>
                </StackPanel>
            </TabItem>
            <TabItem Header="历史">
                <DataGrid ItemsSource="{Binding History.Items}" AutoGenerateColumns="False" Margin="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="时间" Binding="{Binding CreatedAt}" Width="160"/>
                        <DataGridTextColumn Header="文本" Binding="{Binding Text}" Width="*"/>
                        <DataGridTextColumn Header="字数" Binding="{Binding CharacterCount}" Width="70"/>
                        <DataGridTemplateColumn Header="操作" Width="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="复制"
                                            Command="{Binding DataContext.History.CopyCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="统计">
                <DataGrid ItemsSource="{Binding Stats.Days}" AutoGenerateColumns="False" Margin="12">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="日期" Binding="{Binding Date}" Width="120"/>
                        <DataGridTextColumn Header="次数" Binding="{Binding InputCount}" Width="80"/>
                        <DataGridTextColumn Header="成功" Binding="{Binding SuccessCount}" Width="80"/>
                        <DataGridTextColumn Header="失败" Binding="{Binding FailedCount}" Width="80"/>
                        <DataGridTextColumn Header="录音秒数" Binding="{Binding RecordingSeconds}" Width="100"/>
                        <DataGridTextColumn Header="识别秒数" Binding="{Binding ProcessingSeconds}" Width="100"/>
                        <DataGridTextColumn Header="字数" Binding="{Binding CharacterCount}" Width="80"/>
                        <DataGridTextColumn Header="词数" Binding="{Binding WordCount}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="模型">
                <StackPanel Margin="16" Width="520" HorizontalAlignment="Left">
                    <TextBlock Text="模型名称" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Model.ModelName}" Margin="0,4,0,8"/>
                    <TextBlock Text="模型路径" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Model.ModelPath}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                    <TextBlock Text="服务状态" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Model.ServiceState}" Margin="0,4,0,8"/>
                    <TextBlock Text="服务地址" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Model.ServiceAddress}" Margin="0,4,0,8"/>
                    <TextBlock Text="最近操作" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding Model.LastMessage}" TextWrapping="Wrap" Margin="0,4,0,8"/>
                    <TextBlock Text="最近错误" FontWeight="SemiBold" Foreground="DarkRed"/>
                    <TextBlock Text="{Binding Model.LastError}" TextWrapping="Wrap" Foreground="DarkRed" Margin="0,4,0,12"/>
                    <WrapPanel>
                        <Button Content="启动服务" Command="{Binding Model.StartCommand}" Margin="0,0,8,8"/>
                        <Button Content="停止服务" Command="{Binding Model.StopCommand}" Margin="0,0,8,8"/>
                        <Button Content="重启服务" Command="{Binding Model.RestartCommand}" Margin="0,0,8,8"/>
                        <Button Content="健康检查" Command="{Binding Model.HealthCheckCommand}" Margin="0,0,8,8"/>
                    </WrapPanel>
                </StackPanel>
            </TabItem>
            <TabItem x:Name="SettingsTabItem" Header="设置">
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Padding="0,0,0,16">
                    <StackPanel Margin="16" Width="560" HorizontalAlignment="Left">
                    <TextBlock Text="首选语言"/>
                    <ComboBox ItemsSource="{Binding Settings.TranscriptionLanguageOptions}"
                              DisplayMemberPath="DisplayName"
                              SelectedValuePath="Id"
                              SelectedValue="{Binding Settings.PreferredTranscriptionLanguageId}"
                              Margin="0,4,0,4"
                              Width="280"
                              HorizontalAlignment="Left"/>
                    <TextBlock Text="保存后下次听写生效。" Foreground="Gray" Margin="0,0,0,12"/>
                    <TextBlock Text="模型文件路径"/>
                    <Grid Margin="0,4,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding Settings.ModelPath, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Grid.Column="1" Content="浏览…" Margin="8,0,0,0" Padding="12,4"
                                Command="{Binding Settings.BrowseModelPathCommand}"/>
                    </Grid>
                    <TextBlock Text="whisper-server 路径"/>
                    <Grid Margin="0,4,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding Settings.WhisperServerPath, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Grid.Column="1" Content="浏览…" Margin="8,0,0,0" Padding="12,4"
                                Command="{Binding Settings.BrowseWhisperServerPathCommand}"/>
                    </Grid>
                    <TextBlock Text="whisper-server 端口"/>
                    <TextBox Text="{Binding Settings.WhisperServerPort, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,4,0,12" Width="120" HorizontalAlignment="Left"/>
                    <TextBlock Text="whisper-server 线程数"/>
                    <Grid Margin="0,4,0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding Settings.WhisperServerThreadCount, UpdateSourceTrigger=PropertyChanged}"
                                 Width="120"
                                 HorizontalAlignment="Left"
                                 infra:IntegerTextBoxBehavior.IsEnabled="True"/>
                        <Button Grid.Column="1" Content="重置" Margin="8,0,0,0" Padding="12,4"
                                Command="{Binding Settings.ResetWhisperServerThreadCountCommand}"/>
                    </Grid>
                    <TextBlock Foreground="Gray" Margin="0,0,0,12">
                        <Run Text="推荐值："/>
                        <Run Text="{Binding Settings.RecommendedWhisperServerThreadCount, Mode=OneWay}"/>
                        <Run Text="（根据本机 CPU 自动判断；重置后恢复为推荐值）"/>
                    </TextBlock>
                    <TextBlock Text="{Binding Settings.DataDirectoryPath, StringFormat=数据目录：{0}}" Foreground="Gray" Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding Settings.LogsDirectoryPath, StringFormat=日志目录：{0}}" Foreground="Gray" Margin="0,0,0,12"/>
                    <CheckBox Content="客户端启动时自动加载当前模型" IsChecked="{Binding Settings.StartModelOnAppStartup}" Margin="0,0,0,8"/>
                    <CheckBox Content="关闭窗口时最小化到系统托盘" IsChecked="{Binding Settings.MinimizeToTrayOnClose}" Margin="0,0,0,4"/>
                    <TextBlock Text="取消勾选后，点击窗口关闭按钮将直接退出程序。" Foreground="Gray" Margin="20,0,0,12"/>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="保存设置" Command="{Binding Settings.SaveCommand}" Width="100"/>
                        <TextBlock Text="{Binding Settings.LastSavedAtText}"
                                   VerticalAlignment="Center"
                                   Margin="12,0,0,0"
                                   Foreground="Gray"/>
                    </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Debug">
                <StackPanel Margin="16" Width="520" HorizontalAlignment="Left">
                    <TextBlock Text="覆盖状态模拟" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    <WrapPanel>
                        <Button Content="模型加载中" Command="{Binding Debug.ShowLoadingCommand}" Margin="0,0,8,8"/>
                        <Button Content="待录音" Command="{Binding Debug.ShowReadyCommand}" Margin="0,0,8,8"/>
                        <Button Content="正在聆听" Command="{Binding Debug.ShowRecordingCommand}" Margin="0,0,8,8"/>
                        <Button Content="识别中" Command="{Binding Debug.ShowTranscribingCommand}" Margin="0,0,8,8"/>
                        <Button Content="正在输入" Command="{Binding Debug.ShowInjectingCommand}" Margin="0,0,8,8"/>
                        <Button Content="已注入" Command="{Binding Debug.ShowInjectedCommand}" Margin="0,0,8,8"/>
                        <Button Content="识别结果" Command="{Binding Debug.ShowResultCommand}" Margin="0,0,8,8"/>
                        <Button Content="错误状态" Command="{Binding Debug.ShowErrorCommand}" Margin="0,0,8,8"/>
                        <Button Content="隐藏覆盖" Command="{Binding Debug.HideCommand}" Margin="0,0,8,8"/>
                    </WrapPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
